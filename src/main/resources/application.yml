server:
  port: 8080

spring:
  application:
    name: lol-team-balancer
  datasource:
    url: jdbc:mariadb://localhost:3306/lol_team_builder
    username: happyhouse
    password: happyhouse123
    driver-class-name: org.mariadb.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MariaDBDialect
